1. 已有功能
   2. 同名可赋值的字段拷贝
   3. 集合之间拷贝
   4. 对象和mapper转换
   5. 不同命的属性可以转换
   6. 不同类型的属性可以转换
   7. 可以指定某些名称的属性不转换
   8. 可以注入bean，并且引用注入的bean的方法转换属性


2. 待完成功能
   3. 可以指定整个输入参数或者整个输出参数不转换，只转换PropertyMapping指定的部分。
      4. 目前只完成了bean对bean的不转换部分
      5. 需要完成支持Map
   4.是否需要支持多级，级联属性配置。




